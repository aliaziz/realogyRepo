version: 2.1
orbs:
  android: circleci/android@1.0.3
jobs:
  build-and-test:
    executor:
      name: android/android-machine
    steps:
      - checkout
      - android/run-tests:
          test-command: ./gradlew simpson_viewerDebug
workflows:
  testrunner:
    jobs:
      - build-and-test
